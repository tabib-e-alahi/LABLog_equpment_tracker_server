import { RequestHandler } from "express";

const createLog: RequestHandler = async(req, res) =>{
      try {
            const payload = req.body;

            const data = await pris
            
      } catch (error) {
            
      }